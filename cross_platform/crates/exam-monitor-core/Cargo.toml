[package]
name = "exam-monitor-core"
version.workspace = true
edition.workspace = true

[dependencies]
anyhow = "1"
base64 = "0.22"
image = { version = "0.24", default-features = false, features = ["jpeg", "png"] }
screenshots = "0.8"
serde = { version = "1", features = ["derive"] }

